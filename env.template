# SMTP Configuration for Automatic Email Sending
# Copy this file to .env and fill in your actual values
# Or run: cp env.template .env

# SMTP Server Settings (for sending emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true

# IMAP Server Settings (for receiving emails, e.g., HARO)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# Your Email Credentials
# For Gmail, use an App Password (not your regular password)
# Generate one at: https://myaccount.google.com/apppasswords
# These credentials are used for both SMTP (sending) and IMAP (receiving)
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# Email Sender Information
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Build-a-Dress Team

# Notes:
# - For Gmail, you'll need to use an "App Password" instead of your regular password
#   Generate one at: https://myaccount.google.com/apppasswords
# - For other email providers, adjust SMTP_HOST and SMTP_PORT accordingly:
#   - Outlook: smtp-mail.outlook.com:587
#   - Yahoo: smtp.mail.yahoo.com:587
#   - Custom: Check with your email provider
# - After creating .env, you can source it:
#   source .env
#   Or use a tool like python-dotenv to load it automatically

